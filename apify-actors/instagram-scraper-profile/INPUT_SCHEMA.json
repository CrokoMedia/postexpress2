{
  "title": "Instagram Profile Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "username": {
      "title": "Username do Instagram",
      "type": "string",
      "description": "Username do perfil a ser extraído (sem @). Exemplo: frankcosta",
      "editor": "textfield",
      "example": "frankcosta"
    },
    "maxPosts": {
      "title": "Número máximo de posts",
      "type": "integer",
      "description": "Quantos posts extrair do perfil",
      "default": 50,
      "minimum": 1,
      "maximum": 200
    },
    "includeComments": {
      "title": "Incluir comentários",
      "type": "boolean",
      "description": "Se deve extrair comentários dos posts",
      "default": true
    },
    "commentsLimit": {
      "title": "Limite de comentários por post",
      "type": "integer",
      "description": "Quantos comentários extrair de cada post",
      "default": 10,
      "minimum": 0,
      "maximum": 50
    }
  },
  "required": ["username"]
}
