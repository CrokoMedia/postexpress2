# ============================================
# RENDER SECRET FILE: .env.production.local
# ============================================
# INSTRUÇÕES:
# 1. Copie TODO este conteúdo
# 2. No Render Dashboard → Settings → Secret Files
# 3. Add Secret File
# 4. Filename: .env.production.local
# 5. Cole este conteúdo
# 6. Substitua TODOS os valores "SUBSTITUA_AQUI" pelos valores reais
# 7. Save Secret File
# 8. Deploy com "Clear build cache"
# ============================================

# === Supabase PUBLIC (embedded in Next.js bundle) ===
# ⚠️ CRÍTICO: Estas 2 variáveis DEVEM estar aqui para o build funcionar
NEXT_PUBLIC_SUPABASE_URL=https://cddbkqaqgbvjjyxkgzfi.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=SUBSTITUA_AQUI

# === Supabase PRIVATE (server-side only) ===
SUPABASE_URL=https://cddbkqaqgbvjjyxkgzfi.supabase.co
SUPABASE_ANON_KEY=SUBSTITUA_AQUI
SUPABASE_SERVICE_ROLE_KEY=SUBSTITUA_AQUI

# === Cloudinary (upload de imagens) ===
CLOUDINARY_CLOUD_NAME=dwkothqfw
CLOUDINARY_API_KEY=555748429968394
CLOUDINARY_API_SECRET=SUBSTITUA_AQUI
CLOUDINARY_URL=cloudinary://555748429968394:SUBSTITUA_AQUI@dwkothqfw

# === Anthropic (Claude API para geração de conteúdo) ===
ANTHROPIC_API_KEY=SUBSTITUA_AQUI

# === Google AI (Gemini para OCR) ===
GOOGLE_AI_API_KEY=SUBSTITUA_AQUI

# === Mistral AI (OCR alternativo) ===
MISTRAL_API_KEY=SUBSTITUA_AQUI

# === Apify (Instagram scraping) ===
APIFY_API_TOKEN=SUBSTITUA_AQUI

# === UAZapi (WhatsApp Business API) ===
UAZAPI_INSTANCE_ID=SUBSTITUA_AQUI
UAZAPI_TOKEN=SUBSTITUA_AQUI
UAZAPI_WEBHOOK_URL=https://crokolab.onrender.com/api/whatsapp/webhook

# === Google Drive (Export de slides) ===
GOOGLE_DRIVE_CLIENT_EMAIL=SUBSTITUA_AQUI
GOOGLE_DRIVE_PRIVATE_KEY=SUBSTITUA_AQUI
GOOGLE_DRIVE_FOLDER_ID=SUBSTITUA_AQUI

# === Chromium/Remotion (não mudar) ===
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
CHROME_BIN=/usr/bin/chromium
REMOTION_BROWSER_EXECUTABLE=/usr/bin/chromium

# === Next.js (não mudar) ===
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
